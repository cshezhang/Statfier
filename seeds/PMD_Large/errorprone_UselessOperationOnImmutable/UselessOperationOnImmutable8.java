
public class Foo {
    public void foo() {
        String s;
        String s2 = "foo" + s.substring( 0, delimiterIndex ) + "/";
        s2 = "foo" + s.substring( 0, delimiterIndex );
        if (s.trim().length() > 0) {
        }
    }
}
        