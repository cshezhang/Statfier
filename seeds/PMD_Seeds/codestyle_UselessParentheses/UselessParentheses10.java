
public class Test {
    public void testMethod() {
        String oneDay = "day";
        String moreDays = "days";
        int dayCount = 4;
        String description = dayCount + " " + (dayCount == 1 ? oneDay : moreDays);
    }
}
        