package iter0;

public class Foo {
    public void bar() {
        if (foo) {
            StringBuffer buf = new StringBuffer();
            buf.append("hello");
            java.lang.Object x = a("world" + x, buf.toString());
        }
    }
}
        