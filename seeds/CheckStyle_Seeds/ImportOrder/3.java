// OK
// violation, wrong order, caused by above static import
// all static imports comes at bottom
// violation, extra separation in import group
// violation, wrong order, 'javax' should be above 'java' imports
// violation, wrong order, 'com' imports should be at top

public class TestClass {}

