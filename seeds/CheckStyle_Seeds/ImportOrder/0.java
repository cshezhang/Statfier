
// violation, extra separation before import
// and wrong order, comes before 'java.net.URL'.
// violation, extra separation due to above comment
// OK
// violation, wrong order, 'java' should not come after 'org' imports
// violation, wrong order, 'com' imports comes at top
// OK

public class SomeClass {}

